# 🌱 FarmMesh AI

**AI-powered agricultural marketplace for India** now extended with the **AMIE feasibility engine**.

Everyone gets a clean, fast marketplace for farm-fresh produce. Farmers/vendors additionally unlock an **AI vendor workspace**: harvest analysis, price recommendation, buyer matching, supply aggregation, logistics estimation, market intelligence and a conversational copilot.

## 🧭 AMIE — Feasibility Lab (new)

AMIE (Agricultural Market Infeasibility Engine) answers the question no price chart can: **when will this market physically break?** It runs a deterministic, day-by-day cascading simulation of future supply, capacity and demand — no LLM, no magic numbers:

- **Market analysis from live data** — real price observations, demand signals, active listings and buyer requests from the DB (the old `random.uniform` placeholder is gone).
- **Cascading simulation** — each day: arrivals → redirect → processing (FIFO) → storage (bounded by capacity *and* daily intake) → emergency procurement → buyer absorption → spoilage after the perish window. Day *d*'s overflow degrades day *d+1*.
- **Infeasibility detection** — first failure day/node/deficit with a WHAT/WHEN/WHERE/HOW MUCH/WHY explanation, ranked bottlenecks, and P10/P50/P90 supply bands.
- **Critical commitments** — greedy heuristic finding which synchronized farmer lots cause the failure.
- **Intervention lab** — redirect / store / reschedule / process / procure, sized from the measured deficit, each re-simulated with costs and net benefit; best plan shown as a no-intervention vs intervention counterfactual.
- 8 disturbance scenarios (synchronized harvest, processor outage, buyer withdrawal, compound shock…), fixed seed 42 → reproducible results.

**Where:** vendor workspace → *Feasibility Lab* (or `POST /api/vendor/amie/analyze`, `/api/vendor/amie/counterfactual`).

**Tests:** `cd backend && python -m pytest tests/test_feasibility.py -q` — known feasible/infeasible cases, capacity/conservation/time/nonnegativity invariants, monotonicity stress tests (PRD §29/§31/§32).

## Stack (per PRD §22/§78)

| Layer    | Tech |
|----------|------|
| Frontend | Next.js 14 (App Router) · TypeScript · Tailwind CSS |
| Backend  | Python · FastAPI · Pydantic · SQLAlchemy |
| Database | SQLite (PostgreSQL-swappable — one connection-string change) |
| AI       | Deterministic engines (pricing/matching/forecast/optimization) + optional Groq LLM for copilot phrasing |

## Run it

**Terminal 1 — backend (port 8000):**
```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```
On first start it creates `farmmesh.db` and seeds synthetic Kochi/Ernakulam data (§45): 5 vendors, 3 buyers, 15 listings, 8 buyer requests, 180 days of price history + demand signals.

**Terminal 2 — frontend (port 3000):**
```bash
cd frontend
npm install
npm run dev
```

Open **http://localhost:3000**

## Demo accounts

| Role | Email | Password |
|------|-------|----------|
| 🌱 Farmer/vendor | `ravi@farmmesh.demo` | `farm1234` |
| 🛒 Restaurant buyer | `chef@harbourkitchen.demo` | `buyer1234` |
| 🛡️ Admin | `admin@farmmesh.demo` | `admin1234` |

(Other vendors: meera@ / joseph@ / fatima@ / suresh@ `@farmmesh.demo`, same `farm1234`.)

## The two experiences (PRD §1)

- **Buyer path:** sign up → role "buyer" → marketplace → search/filter → listing detail with lot traceability → order → track status.
- **Vendor path:** sign up → role "farmer/vendor" → AI workspace: sell flow (describe harvest → AI crop ID + grade + price range → editable listing → publish), buyer matching per listing, supply aggregation, what-if market simulation, crop plan, net earnings, copilot.

## Architecture rules honored (PRD §5.0/§43/§77A)

- **Server-side RBAC:** vendor/AI endpoints are gated by `require_vendor`; role comes from the DB/JWT, never client flags. A standard user calling `/api/vendor/*` gets **403**.
- **LLM never the source of truth:** the copilot classifies intent → runs deterministic tools (prices, buyers, earnings, forecast) → LLM only phrases the verified JSON. Without `GROQ_API_KEY`, rule-based explanations use the same data.
- **Ranges + confidence, never guarantees:** price outputs are ranges with confidence and reasons (§10/§44); AI grades are labeled "preliminary, subject to buyer verification" (§9).

## Optional: enable the LLM layer

```bash
cd backend
cp .env.example .env   # set GROQ_API_KEY=...
```
Copilot replies are then generated by Groq; provider string shown in the drawer.

## Key endpoints

```
POST /api/auth/signup | login | onboard        GET /api/auth/me
GET  /api/market/listings?search&crop&grade    GET /api/market/listings/{id}
POST /api/listings                             GET /api/listings/mine
POST /api/orders                               GET /api/orders/mine
POST /api/vendor/ai/analyze-harvest            POST /api/vendor/ai/generate-listing
POST /api/vendor/ai/price                      GET  /api/vendor/ai/match/{listing_id}
POST /api/vendor/ai/aggregate                  POST /api/vendor/ai/logistics
GET  /api/vendor/ai/demand/{crop}              GET  /api/vendor/ai/crop-recommendation
GET  /api/vendor/intelligence/outlook          POST /api/vendor/intelligence/simulate
POST /api/vendor/amie/analyze                  POST /api/vendor/amie/counterfactual
GET  /api/vendor/amie/scenarios                POST /api/vendor/copilot
GET  /api/vendor/earnings | dashboard
GET  /api/admin/metrics                        GET  /api/health
```
